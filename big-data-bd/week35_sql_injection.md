# Неделя 35 – SQL-инъекция (уязвимость)

## Что делал
- Создал уязвимый скрипт `vuln.py` на Python
- Подключился к базе данных `testdb` в PostgreSQL
- Ввёл вредоносную строку `Ivan' OR '1'='1`
- Получил всех пользователей из таблицы `users`

## Как это работает
В уязвимом коде пользовательский ввод подставляется прямо в SQL-запрос:

```python
cur.execute(f"SELECT * FROM users WHERE name = '{name}';")

